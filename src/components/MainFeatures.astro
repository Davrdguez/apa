---
---
<section id="features" class="bg-[#f6f5f2] py-24 px-6 text-center">
  <h2 class="text-3xl font-semibold text-black mb-16">
    Principales características de nuestra herramienta APA
  </h2>

  <div class="max-w-6xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-10">

    <!-- Feature 1 -->
    <div class="feature-card opacity-0 translate-y-10 bg-white rounded-xl p-6 shadow hover:shadow-md transition text-left">
      <div class="flex items-center space-x-3 mb-4">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-[#f76300]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
          <polyline points="14 2 14 8 20 8"/>
        </svg>
        <h3 class="text-lg font-bold text-black">Genera portada automáticamente</h3>
      </div>
      <p class="text-gray-700 text-sm">
        Ingresa los datos del trabajo y crea una portada en estilo APA lista para entregar.
      </p>
    </div>

    <!-- Feature 2 -->
    <div class="feature-card opacity-0 translate-y-10 bg-white rounded-xl p-6 shadow hover:shadow-md transition text-left">
      <div class="flex items-center space-x-3 mb-4">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-[#f76300]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path d="M4 4h16v16H4z" />
          <line x1="8" y1="8" x2="16" y2="8" />
          <line x1="8" y1="12" x2="16" y2="12" />
          <line x1="8" y1="16" x2="14" y2="16" />
        </svg>
        <h3 class="text-lg font-bold text-black">Formato APA completo</h3>
      </div>
      <p class="text-gray-700 text-sm">
        Aplicamos márgenes, fuente Times New Roman, interlineado 2.0, y títulos centrados automáticamente.
      </p>
    </div>

    <!-- Feature 3 -->
    <div class="feature-card opacity-0 translate-y-10 bg-white rounded-xl p-6 shadow hover:shadow-md transition text-left">
      <div class="flex items-center space-x-3 mb-4">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-[#f76300]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path d="M2 7c5 0 8 2 10 5 2-3 5-5 10-5" />
          <path d="M12 12v10" />
          <path d="M2 17c5 0 8 2 10 5 2-3 5-5 10-5" />
        </svg>
        <h3 class="text-lg font-bold text-black">Referencias en estilo APA</h3>
      </div>
      <p class="text-gray-700 text-sm">
        Reformateamos tus referencias automáticamente a estilo APA, ordenadas y con sangría francesa.
      </p>
    </div>

    <!-- Feature 4 -->
    <div class="feature-card opacity-0 translate-y-10 bg-white rounded-xl p-6 shadow hover:shadow-md transition text-left">
      <div class="flex items-center space-x-3 mb-4">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-[#f76300]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <rect x="3" y="11" width="18" height="11" rx="2" />
          <path d="M7 11V7a5 5 0 0 1 10 0v4" />
        </svg>
        <h3 class="text-lg font-bold text-black">Privacidad garantizada</h3>
      </div>
      <p class="text-gray-700 text-sm">
        Tu archivo se procesa localmente. No usamos servidores externos ni subimos tu contenido a la nube.
      </p>
    </div>

  </div>
</section>

<!-- Animaciones con anime.js -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/3.2.1/anime.min.js" client:load></script>

<script type="module" client:load>
  const cards = document.querySelectorAll('.feature-card');

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry, index) => {
      if (entry.isIntersecting) {
        anime({
          targets: entry.target,
          opacity: [0, 1],
          translateY: [40, 0],
          duration: 1000,
          delay: index * 150, // animación en cascada
          easing: 'easeOutQuad'
        });
        observer.unobserve(entry.target);
      }
    });
  }, {
    threshold: 0.2
  });

  cards.forEach(card => observer.observe(card));
</script>
